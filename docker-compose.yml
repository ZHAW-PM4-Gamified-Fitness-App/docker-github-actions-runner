services:
  base:
    build:
      context: .
      dockerfile: Dockerfile.base
    image: pixlmint/github-runner-base:latest
  main:
    build:
      context: .
      dockerfile: Dockerfile
    image: pixlmint/github-runner:latest
    volumes:
      - ~/ZHAW/PM4/gamified-fitness:/workspace
    command: ["tail", "-f", "/dev/null"]
